{
  "name": "coldstart-mcp",
  "version": "2.0.0",
  "description": "Local MCP server for AI agent codebase navigation — regex parsing, TF-IDF + PageRank, git co-change",
  "type": "module",
  "bin": {
    "coldstart-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --noEmit false",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3",
    "@types/node": "^22"
  },
  "engines": {
    "node": ">=18"
  }
}
