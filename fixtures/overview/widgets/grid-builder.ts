// Fixture for matched-token display cuts:
// - separator-compound name (`grid-builder`) must NOT appear in [matched]
// - export `buildGrid` lets a SYNONYM query ("create" -> "build") match for
//   ranking while staying out of the [matched] display.
export function buildGrid(): void {}
export const gridConfig = {};
