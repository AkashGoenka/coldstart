---
import Head from '../../layouts/Head.astro';
import SvgDefs from '../../components/SvgDefs.astro';
import Nav from '../../components/Nav.astro';
import Footer from '../../components/Footer.astro';
import '../../styles/tailwind.css';
import '../../styles/tokens.css';
import '../../styles/docs.css';
import '../../styles/how.css';

const title = 'How I benchmark codebase memory, and how you can | coldstart';
const description =
  'The method behind my numbers: where the questions come from, how the right answer gets decided, and the training-data problem that made me throw my first benchmark away. Run it on any skill, rules file, or tool.';

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'TechArticle',
  headline: 'How I benchmark codebase memory, and how you can',
  description,
  url: 'https://coldstartmcp.dev/benchmark/',
  author: { '@type': 'Person', name: 'Akash Goenka' },
  publisher: { '@type': 'Organization', name: 'coldstart' },
  about: 'Benchmarking AI coding agent tools',
  proficiencyLevel: 'Beginner',
};
---
<html lang="en">
<head>
  <Head
    title={title}
    description={description}
    keywords="benchmark AI coding agent, token usage benchmark, codebase memory, measuring agent tools, training data contamination, benchmark methodology, agent token savings"
    canonicalPath="/benchmark/"
    ogType="article"
    ogTitle="How I benchmark codebase memory, and how you can"
    ogDescription="Where the questions come from, how the right answer gets decided, and the training-data problem that made me throw my first benchmark away."
    themeColor="#070a11"
  />
  <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,opsz,wght@0,6..72,400;0,6..72,500;1,6..72,400;1,6..72,500&family=IBM+Plex+Sans:wght@400;500;600&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet" />
</head>
<body class="how-page">
<SvgDefs />
<Nav variant="how" />

<!-- ===================== HERO ===================== -->
<header class="how-hero">
  <div class="wrap">
    <div class="how-eyebrow">Benchmark method</div>
    <h1>I threw my first benchmark away. This is the one that replaced it.</h1>
    <p class="lead">
      My first set of numbers came from Kafka, Django and Mastodon, and they were wrong in a way I
      couldn't see until I went looking for it. Not wrong by a little. Wrong because the model had
      already read those codebases, so the arm with no tool installed was being helped by something
      I wasn't measuring. This page is the method I rebuilt after that: where the questions come
      from, how the right answer gets decided, what I throw away, and how you can run the same thing
      on a skill or a rules file or a tool that has nothing to do with mine.
    </p>
    <a class="how-back" href="/">← back to the overview</a>
  </div>
</header>

<!-- ===================== 1. WHAT IS MEASURED ===================== -->
<section class="how-sec">
  <div class="wrap">
    <div class="how-sec-head">
      <h2>What gets measured: finding the files, not finishing the job</h2>
      <p>
        A session has two halves. The agent works out which files matter, and then it reads,
        reasons and edits them. Only the first half is what a navigation or memory tool touches. The
        second half is the same work whichever setup you're running, so measuring it adds the same
        cost to both sides and makes the difference between them look smaller than it is. So every
        question here asks the agent to find files, and stops there.
      </p>
    </div>

    <div class="flags">
      <div class="flag-row">
        <span class="fk">The task</span>
        <span class="fv">Every prompt asks where something lives, not for a fix. The answer is a
        list of file paths, and the instructions say so, because an agent handed a bug report will
        start editing if you let it.</span>
      </div>
      <div class="flag-row">
        <span class="fk">Ground truth</span>
        <span class="fv">The source files the pull request actually changed, written down before
        either arm runs. Nothing is graded by hand afterwards, so I can't decide what counted once I
        know which side did better.</span>
      </div>
      <div class="flag-row">
        <span class="fk">Reported</span>
        <span class="fv">Tokens and turns across the whole session, and how many of the right files
        the agent named. Both, always. A tool that reports only cost is rewarded for making the agent
        give up sooner.</span>
      </div>
    </div>

    <div class="how-aside">
      <b>Why the whole session.</b> Around 90% of what a session costs is cached context replayed on
      every turn. Shrink what one command returns and the agent can just ask for more of it, so a
      tool can make its own output smaller and your bill bigger at the same time. I wrote about that
      at length in <a href="/blog/why-most-token-savings-tools-lie/">why most token savings tools
      lie</a>.
    </div>
  </div>
</section>

<!-- ===================== 2. WHERE THE QUESTIONS COME FROM ===================== -->
<section class="how-sec">
  <div class="wrap">
    <div class="how-sec-head">
      <h2>The questions have to be written by someone who didn't know the answer</h2>
      <p>
        This is the part that decides whether the whole thing measures anything. If the prompt names
        the file, both setups find it straight away and there is nothing left to compare. I tried
        generating questions from commit messages and pull request descriptions first, and threw that
        out. They're written afterwards by the person who did the work, so they name classes and
        paths without thinking about it.
      </p>
      <p>
        Issue reports are the opposite. Someone writes them while describing a symptom, before
        anyone knows where the cause lives, so they use the project's own vocabulary without pointing
        at a file. That's why they work. It isn't careful editing on my part, it's who was holding
        the pen.
      </p>
    </div>

    <div class="flags">
      <div class="flag-row">
        <span class="fk">1 · select</span>
        <span class="fv">Closed issues whose pull request was merged, touching between two and eight
        source files. One-file fixes are just a grep. Fifty-file refactors make the overlap score
        meaningless.</span>
      </div>
      <div class="flag-row">
        <span class="fk">2 · scrub</span>
        <span class="fv">Take out file paths, stack frames, code blocks, screenshots and issue
        numbers. What's left should still say what's broken, and not where it lives.</span>
      </div>
      <div class="flag-row">
        <span class="fk">3 · reshape</span>
        <span class="fv">Turn the report into a question about where something lives, keeping the
        author's words and adding none of my own. The original and the rewrite are both saved, so
        anyone can check what changed.</span>
      </div>
      <div class="flag-row">
        <span class="fk">4 · drop</span>
        <span class="fv">Throw away anything too vague to navigate from, and anything that lost most
        of itself in the previous step, which is what happens when an issue was really just a
        pointer to a file.</span>
      </div>
    </div>

    <p class="sec-tail">
      A lot of candidates don't survive that, and on one repository more were thrown out than kept.
      The discarded ones get published with the reason attached, because a list of what I removed is
      the only way you can tell that what's left wasn't picked to make the result look good.
    </p>
  </div>
</section>

<!-- ===================== 3. THE BIAS FINDING ===================== -->
<section class="how-sec">
  <div class="wrap">
    <div class="how-sec-head">
      <h2>The problem that invalidated my first benchmark</h2>
      <p>
        Kafka, Django and Mastodon looked like good choices. Large, real, well organised. They are
        also three of the most written-about codebases on the internet, and that turned out to matter
        more than anything about how they're built.
      </p>
      <p>
        The test is simple enough that you can run it on your own results. Take the arm with no tool
        installed and look at its first move, before it has opened a single file. Pull out the
        internal class names it used. Subtract anything the prompt gave it. Whatever is left didn't
        come from the codebase, because the agent hadn't looked at the codebase yet. It came from the
        model already knowing the project.
      </p>
    </div>

    <div class="flags">
      <div class="flag-row">
        <span class="fk">Kafka</span>
        <span class="fv"><b>21 of 25 sessions (84%)</b> named internal class names before opening a
        file.</span>
      </div>
      <div class="flag-row">
        <span class="fk">Django</span>
        <span class="fv"><b>7 of 14 (50%)</b>.</span>
      </div>
      <div class="flag-row">
        <span class="fk">Mastodon</span>
        <span class="fv"><b>6 of 18 (33%)</b>.</span>
      </div>
    </div>

    <p>
      This isn't noise that averages out. It pushes the result one way. Knowing the codebase makes
      the no-tool arm cheaper, because an agent that already knows where to look skips the searching
      it would otherwise pay for. The better the model knows your benchmark repository, the smaller
      any tool's improvement is going to look.
    </p>
    <p>
      My savings on those three ranked Mastodon 43%, Django 28%, Kafka 15%, which is the leak rates
      above in reverse. I had been reading that ranking as something about languages and codebase
      structure. Most of it was about what the model had already read.
    </p>

    <div class="how-aside">
      <b>What I did about it.</b> Renaming everything to stop the model recognising it needs
      consistent rewrites across declarations, imports, build config and tests, and a half-done
      rename leaves the no-tool arm grepping for names that no longer match, which corrupts the
      measurement rather than cleaning it. So I retired those three repositories and rebuilt on four
      that get written about less. The old numbers aren't anywhere on this site, because I don't
      believe them.
    </div>

    <p class="sec-tail">
      If you're reading someone else's benchmark, this is the first thing to ask about it, and it
      applies to mine as much as anyone's. A result measured on a famous repository is measuring what
      the model remembers as much as what the tool does.
    </p>
  </div>
</section>

<!-- ===================== 4. THE NUMBERS ===================== -->
<section class="how-sec">
  <div class="wrap">
    <div class="how-sec-head">
      <h2>What it says about coldstart</h2>
      <p>
        Two repositories run under the method above. Arches is a Python and Django application. JMRI
        is Java, considerably more verbose per file and with a heavier build surface. Same method,
        same tool, same day.
      </p>
    </div>

    <div class="flags">
      <div class="flag-row"><span class="fk">Arches · Python</span><span class="fv"><b>64% fewer tokens</b> than the no-tool baseline across 27 queries, with recall two points better, because the baseline sometimes ran out of turns before finding the right file at all.</span></div>
      <div class="flag-row"><span class="fk">JMRI · Java</span><span class="fv"><b>31% fewer tokens</b> across 25 queries, with recall at parity. The first run said 39%, on a machine that was doing other work at the time. Rerun in isolation it came out eight points lower, and 31% is the number.</span></div>
    </div>

    <p class="sec-tail">
      Those two disagree, and I'm not going to average them. One tidier figure would hide the only
      interesting thing here, which is that Java's verbosity and build tooling change what a single
      turn costs, and that changes how much a tool which reduces turns can save you. Both query
      lists, the files a correct answer needed, and the candidates I threw out are published, so the
      run can be repeated against my own material.
    </p>

  </div>
</section>

<!-- ===================== 5. RUN IT YOURSELF ===================== -->
<section class="how-sec">
  <div class="wrap">
    <div class="how-sec-head">
      <h2>Run it on something that isn't mine</h2>
      <p>
        None of this is specific to coldstart. The same setup measures a skill, a rules file, an MCP
        server, or a change you made to your own prompt. Take two copies of one repository, install
        the thing you're testing in exactly one of them, ask the same questions in both, and use a
        fresh session for every question so a later prompt never rides on what an earlier one already
        loaded.
      </p>
    </div>

    <div class="flags">
      <div class="flag-row">
        <span class="fk">Measuring</span>
        <span class="fv"><a href="https://github.com/AkashGoenka/convotokens">convotokens</a> reads
        your own transcripts off your own disk and reports tokens and turns for the whole session,
        subagents included. It makes no network calls and won't invent a dollar figure for you.</span>
      </div>
      <div class="flag-row">
        <span class="fk">One prompt, one session</span>
        <span class="fv">Otherwise your third question is cheap because the first two already loaded
        the context, and what you've measured is the order you asked things in.</span>
      </div>
      <div class="flag-row">
        <span class="fk">Same model, both arms</span>
        <span class="fv">Subagents included. If one side ran a mix of models and the other didn't,
        you've measured the models.</span>
      </div>
      <div class="flag-row">
        <span class="fk">Repeat before believing</span>
        <span class="fv">Agent runs vary on their own. Run one side several times without changing
        anything and look at how much it moves, before you believe a difference between the two
        sides.</span>
      </div>
    </div>

    <div class="how-aside">
      <b>Your own private codebase is the best thing to run this on.</b> The problem in the section
      above can't happen in a repository the model has never seen. If you're deciding whether a tool
      is worth it for your team, your own closed code gives you a cleaner answer than any public
      benchmark will.
    </div>
  </div>
</section>

<!-- ===================== 6. WHAT WE DON'T CLAIM ===================== -->
<section class="how-sec">
  <div class="wrap">
    <div class="how-sec-head">
      <h2>What this does not tell you</h2>
    </div>
    <div class="flags">
      <div class="flag-row"><span class="fk">Not whether the fix was good</span><span class="fv">This measures finding the right files. Whether the patch that followed was any good is a different experiment, and I haven't run it.</span></div>
      <div class="flag-row"><span class="fk">Not your codebase</span><span class="fv">Two repositories, two languages, and they already disagree by 33 points. Your layout and conventions will move the number again, in either direction.</span></div>
      <div class="flag-row"><span class="fk">Not a bill</span><span class="fv">These are token counts, not dollars. On a subscription any conversion to money is something I'd be making up.</span></div>
    </div>

    <p class="sec-tail">
      Two longer pieces sit behind this method, both about the ways a benchmark misleads the person
      running it: <a href="/blog/why-most-token-savings-tools-lie/">why most token savings tools
      lie</a> and <a href="/blog/how-your-own-benchmark-lies-to-you-first/">how your own benchmark
      lies to you first</a>.
    </p>
  </div>
</section>

<hr class="thread" />
<Footer variant="how" />

<style>
  /* how.css only ever styled single-paragraph section heads, so consecutive
     paragraphs collapse into one block. Page-scoped rather than edited into
     how.css, which four other pages depend on. */
  .how-sec-head p + p, .how-sec > .wrap > p + p { margin-top: 14px; }
  .how-sec > .wrap > p { color: var(--muted); font-size: 16.5px; }
  /* The closing line of a section: quieter than body copy, no rule above it. */
  .sec-tail { margin-top: 26px; color: var(--muted); font-size: 16.5px; max-width: 680px; }

  /* ONE body measure. how.css stacks several - .wrap 940, .flags 892,
     .how-sec-head/.how-aside 680 - which is fine on /vs/vector-rag/ where wide
     figures fill the container, but this page is prose plus six table blocks,
     so every paragraph ended ~210px short of the rule beneath it and the column
     read as a phone layout stranded in a desktop window. Bring the tables to the
     prose measure rather than narrowing .wrap: the wrap is what puts this page's
     left edge in the same place as /how-it-works/ and /vs/vector-rag/, so a
     reader moving between them doesn't see the text jump sideways. */
  .how-sec .flags, .how-sec .flag-row { max-width: 680px; }
</style>
</body>
</html>
